{
  "name": "steeler-logbook-sync-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate:local": "wrangler d1 migrations apply steeler_logbook_sync --local",
    "d1:migrate:remote": "wrangler d1 migrations apply steeler_logbook_sync --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
